// IF and ELSE
let clima = "soleado";
let clima2 = "lluvioso";
let horario = 3;

if (clima === "soleado" && horario < 6) {
    console.log('Puedes salir');
};

if (clima2 === "soleado" && horario < 6) {
    let message = 'demos un paseo';
    console.log(`EL clima esta ${clima}, entonces ${message}`);
} else {
    console.log('No puedes salir');
};

// TODO
// falta el 3, 4, y 5