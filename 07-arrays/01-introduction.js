// ARRAYS

/*
    Arrays are a very special type of data.
*/

let myPets = ['koen', 'Muñeca', 'azabache'];
console.log(myPets);

// myArray.push('Que hubo pues');
// console.log(myArray[0]);
// myArray.splice(3, 4);

for(let i = 0; i < myPets.length; i++) {
    console.log(`My pet ${myPets[i]}`);
}